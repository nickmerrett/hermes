FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source and test files
COPY . .

# Default command runs tests
CMD ["npm", "run", "test", "--", "--run"]
